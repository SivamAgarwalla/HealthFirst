import React, { useEffect } from 'react';
import { View, Text, Stylesheet } from 'react-native';

const DiseaseSummary = ({ recordings }) => {
  useEffect(() => {}, []);
  return (
    <View>
      <Text> DiseaseSummary </Text>
    </View>
  );
};

export default DiseaseSummary;
